import SeriesList from '@components/series/SeriesList';
import ListLayout from '@components/SubLayout';

function Series() {
  return (
    <ListLayout>
      <SeriesList />
    </ListLayout>
  );
}

export default Series;
